# Credentials and access

This file intentionally contains no credentials.

External mutations require authenticated access to GitHub `dsmarkin/1079-last-night` and the existing Railway project/service IDs in `handoff/README.md`. Use the host's authenticated connectors or the user's own login. Never place tokens, cookies, `.env` values, or browser credentials in Git, the archive, prompts, URLs, or chat.
