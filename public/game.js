import * as THREE from 'https://cdn.jsdelivr.net/npm/three@0.180.0/build/three.module.js';
const canvas=document.querySelector('#game'),renderer=new THREE.WebGLRenderer({canvas,antialias:true});renderer.setPixelRatio(Math.min(devicePixelRatio,1.5));renderer.shadowMap.enabled=true;const scene=new THREE.Scene();scene.background=new THREE.Color(0x81919a);scene.fog=new THREE.FogExp2(0xa9b4b8,.0007);const camera=new THREE.PerspectiveCamera(62,innerWidth/innerHeight,.2,6000);scene.add(new THREE.HemisphereLight(0xd9e6ea,0x293337,1.8));const sun=new THREE.DirectionalLight(0xeaf4f7,2.2);sun.position.set(-600,900,300);scene.add(sun);
const Z=13,TX=5448,TY=2296,TILE=256;let heights=null,minH=1e9,maxH=-1e9,terrain=null;const tileMeters=40075016.686*Math.cos(61.762*Math.PI/180)/2**Z;const scale=tileMeters/TILE;
function ll(lat,lon){const n=2**Z,xf=(lon+180)/360*n,yf=(1-Math.asinh(Math.tan(lat*Math.PI/180))/Math.PI)/2*n;return{x:(xf-TX)*tileMeters-tileMeters/2,z:(yf-TY)*tileMeters-tileMeters/2,u:xf-TX,v:yf-TY}}
const P={tent:ll(61.758618,59.429409),cedar:ll(61.76477,59.4549787),ravine:ll(61.76506217,59.45401969)};
function hAt(x,z){if(!heights)return 0;const px=Math.max(0,Math.min(255,Math.round((x+tileMeters/2)/scale))),py=Math.max(0,Math.min(255,Math.round((z+tileMeters/2)/scale)));return heights[py*TILE+px]-minH}
async function buildTerrain(){document.querySelector('#loading').style.display='block';try{const img=new Image();img.crossOrigin='anonymous';img.src=`https://s3.amazonaws.com/elevation-tiles-prod/terrarium/${Z}/${TX}/${TY}.png`;await img.decode();const c=document.createElement('canvas');c.width=c.height=TILE;const ctx=c.getContext('2d');ctx.drawImage(img,0,0);const d=ctx.getImageData(0,0,TILE,TILE).data;heights=new Float32Array(TILE*TILE);for(let i=0;i<heights.length;i++){const h=d[i*4]*256+d[i*4+1]+d[i*4+2]/256-32768;heights[i]=h;minH=Math.min(minH,h);maxH=Math.max(maxH,h)}const geo=new THREE.PlaneGeometry(tileMeters,tileMeters,255,255);geo.rotateX(-Math.PI/2);const pos=geo.attributes.position;for(let i=0;i<pos.count;i++){const x=pos.getX(i),z=pos.getZ(i);pos.setY(i,hAt(x,z))}geo.computeVertexNormals();terrain=new THREE.Mesh(geo,new THREE.MeshStandardMaterial({color:0xc7d0d1,roughness:1,flatShading:false}));scene.add(terrain)}catch(e){console.warn('DEM fallback',e);minH=0;heights=new Float32Array(TILE*TILE);const g=new THREE.PlaneGeometry(tileMeters,tileMeters);g.rotateX(-Math.PI/2);terrain=new THREE.Mesh(g,new THREE.MeshStandardMaterial({color:0xc7d0d1}));scene.add(terrain)}document.querySelector('#loading').style.display='none';decorate()}
function pine(x,z,s=1){const g=new THREE.Group(),m=new THREE.MeshStandardMaterial({color:0x18312c}),tm=new THREE.MeshStandardMaterial({color:0x3a2d25});const t=new THREE.Mesh(new THREE.CylinderGeometry(.18,.28,3,6),tm);t.position.y=1.5;g.add(t);for(let i=0;i<3;i++){const q=new THREE.Mesh(new THREE.ConeGeometry(1.7-i*.3,3.4,7),m);q.position.y=3+i*1.2;g.add(q)}g.position.set(x,hAt(x,z),z);g.scale.setScalar(s);scene.add(g)}
function marker(pos,type){let m;if(type==='tent'){m=new THREE.Mesh(new THREE.ConeGeometry(5,3.5,4),new THREE.MeshStandardMaterial({color:0x56685a}));m.scale.z=2;m.rotation.y=Math.PI/4}else if(type==='cedar'){pine(pos.x,pos.z,3);return}else{m=new THREE.Mesh(new THREE.TorusGeometry(4,.5,8,24),new THREE.MeshStandardMaterial({color:0x513e36}));m.rotation.x=Math.PI/2}m.position.set(pos.x,hAt(pos.x,pos.z)+2,pos.z);scene.add(m)}
function decorate(){let seed=1959;const rnd=()=>((seed=Math.imul(seed,1664525)+1013904223>>>0)/4294967296);for(let i=0;i<430;i++){const x=(rnd()-.5)*tileMeters,z=(rnd()-.5)*tileMeters;if(hAt(x,z)+minH>735)continue;pine(x,z,.7+rnd()*1.5)}marker(P.tent,'tent');marker(P.cedar,'cedar');marker(P.ravine,'ravine');spawnLocal()}
const players=new Map();function makeAvatar(color=0x9a5039){const g=new THREE.Group();const body=new THREE.Mesh(new THREE.CapsuleGeometry(.48,1.25,4,8),new THREE.MeshStandardMaterial({color}));body.position.y=1.15;g.add(body);const pack=new THREE.Mesh(new THREE.BoxGeometry(.7,.9,.35),new THREE.MeshStandardMaterial({color:0x39413d}));pack.position.set(0,1.35,.42);g.add(pack);scene.add(g);return g}const me=makeAvatar(0xa95438);me.visible=false;
let yaw=0,pitch=.25,active=false,ready=false;const keys={};function spawnLocal(){me.position.set(P.tent.x,hAt(P.tent.x,P.tent.z)+1,P.tent.z+15);me.visible=true;ready=true}
const socket=io();document.querySelector('#go').onclick=()=>{active=true;document.querySelector('#intro').style.display='none';document.querySelector('#hud').style.display='block';document.querySelector('#minimap').style.display='block';canvas.requestPointerLock();socket.emit('join',{room:document.querySelector('#room').value,name:document.querySelector('#name').value})};socket.on('state',ps=>ps.forEach(p=>p.id!==socket.id&&remote(p)));socket.on('joined',p=>remote(p));socket.on('move',p=>{const g=players.get(p.id)||remote(p);g.position.set(p.x,hAt(p.x,p.z)+1,p.z);g.rotation.y=p.ry});socket.on('left',id=>{const g=players.get(id);if(g)scene.remove(g);players.delete(id)});function remote(p){const g=makeAvatar(0x526c7c);g.position.set(p.x,hAt(p.x,p.z)+1,p.z);players.set(p.id,g);return g}
addEventListener('keydown',e=>{keys[e.code]=true;if(e.code==='KeyM'&&!e.repeat)toggleMap()});addEventListener('keyup',e=>keys[e.code]=false);addEventListener('mousemove',e=>{if(document.pointerLockElement===canvas){yaw-=e.movementX*.0025;pitch=Math.max(-.15,Math.min(.8,pitch-e.movementY*.002))}});canvas.onclick=()=>active&&canvas.requestPointerLock();document.querySelector('#closemap').onclick=toggleMap;function toggleMap(){const b=document.querySelector('#bigmap'),open=b.style.display==='grid';b.style.display=open?'none':'grid';if(!open)document.exitPointerLock();else if(active)canvas.requestPointerLock()}
function mapPos(el){const px=(me.position.x+tileMeters/2)/tileMeters*100,py=(me.position.z+tileMeters/2)/tileMeters*100;el.style.left=px+'%';el.style.top=py+'%';el.style.transform=`translate(-50%,-50%) rotate(${-yaw}rad)`}
const clock=new THREE.Clock();let send=0;function loop(){requestAnimationFrame(loop);const dt=Math.min(clock.getDelta(),.05);if(active&&ready){const f=(keys.KeyW?1:0)-(keys.KeyS?1:0),r=(keys.KeyD?1:0)-(keys.KeyA?1:0);if(f||r){const a=yaw+Math.atan2(r,f),sp=(keys.ShiftLeft?13:7)*dt;me.position.x-=Math.sin(a)*sp;me.position.z-=Math.cos(a)*sp;me.rotation.y=a;me.position.y=hAt(me.position.x,me.position.z)+1}const dist=7.5,height=3.6;const target=new THREE.Vector3(me.position.x,me.position.y+1.2,me.position.z);camera.position.set(target.x+Math.sin(yaw)*Math.cos(pitch)*dist,target.y+height+Math.sin(pitch)*dist,target.z+Math.cos(yaw)*Math.cos(pitch)*dist);camera.lookAt(target);send+=dt;if(send>.06){socket.emit('move',{x:me.position.x,z:me.position.z,ry:me.rotation.y});send=0}mapPos(document.querySelector('#you'));mapPos(document.querySelector('#youBig'));const dc=Math.hypot(me.position.x-P.cedar.x,me.position.z-P.cedar.z),dtent=Math.hypot(me.position.x-P.tent.x,me.position.z-P.tent.z),dr=Math.hypot(me.position.x-P.ravine.x,me.position.z-P.ravine.z);document.querySelector('#place').textContent=dc<45?'Кедр':dr<45?'Овраг':dtent<60?'Место палатки':'Склон Холатчахля';document.querySelector('#online').textContent=`${players.size+1} в экспедиции`}renderer.setSize(innerWidth,innerHeight,false);camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.render(scene,camera)}buildTerrain();loop();